defaults:
  - model: default
  - dataset: default
  - training: default
  - _self_

seed: 42
device: cuda   # or cpu

hydra:
  job:
    chdir: false   # keep CWD as the project directory so relative paths work
