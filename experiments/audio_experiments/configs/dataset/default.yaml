text_corpora_root: dataset/text_corpora
local_audio_root: .
sample_rate: 16000
max_context_turns: 20      # must match model.max_context_turns

val_split: 0.15            # fraction of conversations held out for validation
split_seed: 42             # separate from global seed so data split is stable

embedding_cache_dir: .cache/embeddings
