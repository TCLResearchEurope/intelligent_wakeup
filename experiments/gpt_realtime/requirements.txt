# Note: torch and torchaudio must be installed first with the correct CUDA variant.
# See https://pytorch.org/get-started/locally/ for the right index URL.
# Example (CUDA 12.6):
#   pip install torch torchaudio --index-url https://download.pytorch.org/whl/cu126

whisperx>=3.8.2
torchvision>=0.23.0  # must match installed torch version

# evaluate_corpus.py
pyaudio>=0.2.14
websockets>=13.0
